properties:
  op:
    type: string
    enum:
      - replace
      - add
      - remove
  path:
    type: string
  value:
    type:
      - object
      - array
      - string
      - number
      - integer
      - boolean
      - "null"
    description: Value to be used in the patch operation
