type: object
description: Opaque reference to an EvalCard artifact stored externally.
properties:
  type:
    type: string
    description: Storage backend identifier (e.g. "oci", "mlflow").
  ref:
    type: string
    description: Opaque reference within the storage backend.
required:
  - type
  - ref
