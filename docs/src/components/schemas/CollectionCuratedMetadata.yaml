type: object
description: UI-specific display metadata for curated collections. Set via operator YAML only; not settable via API.
properties:
  icon:
    type: string
    description: Identifier for the collection card icon.
  is_featured:
    type: boolean
    description: >
      When true, this collection is shown prominently in the Curated Collections UI.
